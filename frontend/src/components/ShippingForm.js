// After the order is built, this passes the shipping address and when the user clicks the purchase button, the order is sent to the server for processing and completing the transaction process.

const ShippingForm = () => {
    // ToDo
}

export default ShippingForm;